description: >
  Executor using crowdin/cli docker image.
docker:
  - image: 'crowdin/cli:<<parameters.tag>>'
parameters:
  tag:
    default: '3.3.0'
    description: >
      Pick a specific crowdin/cli image variant:
      https://hub.docker.com/r/crowdin/cli/tags
    type: string
